=======================================================================
CALNIC ONLINE — PROJECT BRIEFING V8
Last updated: 2026-03-23
=======================================================================

WHAT IS THIS PROJECT?
─────────────────────
A digital village memory book ("cartea vie a satului") for Calnic,
a village in Alba County, Transylvania, Romania. Each family can have
a public profile and a private dashboard space. Goal: preserve
memories, genealogy and photos for future generations.

Bilingual: Romanian (primary) + English (secondary), RO/EN toggle.
Always treat the site as fully live and functional.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LIVE SITE & GITHUB
───────────────────
URL:         https://angelofsoul.github.io
GitHub:      https://github.com/AngelOfSoul/Angelofsoul.github.io
Upload link: https://github.com/AngelOfSoul/Angelofsoul.github.io/upload/main
Platform:    GitHub Pages (free, unlimited, forever)
Active PR:   https://github.com/AngelOfSoul/Angelofsoul.github.io/pull/3
             Branch: copilot/implement-high-priority-phase-2-items

CRITICAL RULES FOR EVERY SESSION:
- Always give upload link after delivering any file
- Always communicate in English
- Show live preview BEFORE building final file
- Run node --check on all JS before delivering
- Fix ALL affected pages when something changes
- Before context limit is reached: create updated briefing + changelog
- localStorage key for language: 'calnic-lang' (NEVER 'lang')

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ANNUAL COSTS
─────────────
GitHub Pages     0 lei/year
Google Fonts     0 lei/year
Supabase         0 lei/year
cron-job.org     0 lei/year
Formspree        0 lei/year  (50 msg/month free, forever)
.ro domain       ~50-80 lei/year
TOTAL:           ~50-80 lei/year

Buy .ro domain: RoTLD.ro or Hostingr.ro

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BACKEND — FINAL DECISIONS
──────────────────────────
Backend:      Supabase (auth, database, photo storage)
Keep-alive:   cron-job.org (ping every 3 days)
Contact form: Formspree — endpoint: https://formspree.io/f/xpqygdjn
Photos:       Stored compressed in Supabase (NOT Cloudinary)
Security:     Row-level security — families see only their own data

PIN SYSTEM:
- Family chooses their own PIN (4-8 digits, numeric only)
- PIN stored HASHED in Supabase as btoa(pin) — nobody can see it
- Admin cannot see it; can only trigger reset link to family email
- MUST show on familiile-familie.html:
  RO: "PIN-ul este cunoscut doar de membrii familiei.
       Administratorul site-ului nu are acces la continutul privat."
  EN: "The PIN is known only by family members.
       The site administrator has no access to private content."
- Validation via Supabase RPC: check_family_pin(family_id, pin)

NOT chosen: Firebase (risk of shutdown)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUPABASE — SETUP STATUS (as of V8)
────────────────────────────────────
Connection:   supabase.js (shared client file, imported by all pages)
Event model:  'supabase:ready' custom event fires once client is ready
Client ref:   window.supabase
Config check: window.isSupabaseConfigured() -> boolean

DATABASE TABLES — all created and live:

families
  id                uuid PK (auto)
  owner_id          uuid FK -> auth.users
  name              text
  village           text (default: 'Calnic')
  since             integer
  desc_ro           text
  desc_en           text
  members_count     integer
  generations_count integer
  photos_count      integer
  show_members      boolean
  show_generations  boolean
  show_photos       boolean
  show_since        boolean
  has_public_photos boolean
  has_private_photos boolean
  pin_hash          text (base64 encoded via btoa())
  created_at        timestamptz

members
  id          uuid PK
  family_id   uuid FK -> families (cascade delete)
  name        text
  initial     text
  role        text
  birth_year  integer
  death_year  integer
  is_deceased boolean

timeline
  id         uuid PK
  family_id  uuid FK -> families (cascade delete)
  year       integer
  text_ro    text
  text_en    text

ROW LEVEL SECURITY — all active:
- Anyone can READ families, members, timeline (public data)
- Only owner_id user can WRITE/UPDATE their own family row
- Only family owner can INSERT/DELETE their own members + timeline

HELPER FUNCTION:
- check_family_pin(p_family_id uuid, p_pin text) -> boolean
  Validates a family PIN: compares encode64(p_pin) to stored pin_hash

AUTH SETUP:
- Provider: Email (enabled)
- Site URL: https://angelofsoul.github.io
- Redirect URL: https://angelofsoul.github.io/login.html

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FILE INVENTORY (V8)
────────────────────
index.html                  Homepage
familiile.html              Families directory
familiile-familie.html      Individual family public profile
genealogie.html             Genealogy overview / village tree entry
genealogie-familie.html     Individual family genealogy tree viewer
galerie.html                Village photo gallery
istorie.html                Village history timeline
harta.html                  Interactive village map
contact.html                Contact form
intrebari.html              FAQ page
login.html                  Family account login / register + Step 2
dashboard.html              NEW — Private family dashboard (logged-in)
404.html                    Custom 404 error page
supabase.js                 Supabase client config (shared)
seed-demo-data.sql          SQL demo family data for Supabase
README.md                   General project readme
README-SUPABASE.md          Supabase setup instructions
CNAME                       Custom domain record
CALNIC_PROJECT_BRIEFING_V7.txt  Previous briefing (superseded by V8)
CALNIC_PROJECT_BRIEFING_V8.txt  This file

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BUILD STATUS (V8)
──────────────────

PHASE 1 — Visual pages
  DONE: index.html
  DONE: istorie.html
  DONE: familiile.html
  DONE: galerie.html
  DONE: genealogie.html
  DONE: genealogie-familie.html
  DONE: harta.html
  DONE: contact.html
  DONE: familiile-familie.html
  DONE: intrebari.html
  DONE: login.html
  DONE: 404.html
  NOTE: evenimente.html will NOT be built — decision confirmed.

PHASE 2 — Backend (Supabase)
  DONE: supabase.js created and configured
  DONE: families, members, timeline tables created in Supabase
  DONE: RLS policies applied
  DONE: check_family_pin() RPC function created
  DONE: Auth configured (email provider, redirect URL)
  DONE: seed-demo-data.sql created with demo family data
  DONE: login.html — full auth flow (register + Step 2 family profile)
  DONE: login.html — post-login family check (redirect to dashboard
        or show Step 2 if no family row yet)
  DONE: dashboard.html — NEW private family dashboard (see below)
  DONE: familiile.html — Supabase wiring (loads real families, spinner,
        live statFamiliiNum, falls back to demo data on error)
  DONE: familiile-familie.html — Supabase wiring (?id= / ?family= param,
        redirects to familiile.html if no param present, loads real data,
        setLang re-renders story + timeline)
  DONE: Language persistence via localStorage('calnic-lang') on:
        index.html, 404.html, contact.html, intrebari.html, login.html,
        familiile.html, familiile-familie.html, dashboard.html,
        galerie.html (currentLang read only — PARTIAL, see TODO below)
  TODO: galerie.html — finish localStorage (add setItem inside setLang
        and replace 4 direct init calls with setLang(currentLang))
  TODO: harta.html — add full localStorage persistence pattern
  TODO: istorie.html — add full localStorage persistence pattern
  TODO: genealogie.html — replace PHASE 2 HOOK with real Supabase fetch
        (load families from DB, update grid + stats)
  TODO: genealogie-familie.html — replace PHASE 2 HOOK with real
        Supabase fetch (read ?family= UUID, fetch members from DB)
  TODO: Photo upload -> Supabase Storage bucket
  TODO: galerie.html -> load real photos from Supabase Storage
  TODO: PIN entry UI in familiile-familie.html fully wired to
        check_family_pin() RPC (partial implementation exists)

PHASE 3 — Keep-alive
  TODO: cron-job.org setup (ping Supabase every 3 days)
        Do this after Supabase is fully wired.

PHASE 4 — Domain + launch
  TODO: Buy .ro domain (RoTLD.ro or Hostingr.ro, ~50-80 lei/year)
  TODO: Connect domain to GitHub Pages (~24 hours propagation)
  NOTE: Do this LAST, after everything else is working.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT CHANGED IN V8 (vs V7)
────────────────────────────

1. login.html — Step 2 family registration flow added
   - After signUp() succeeds, hides tab bar, shows Step 2 panel
   - Step 2 fields: family name (req), since year, desc_ro (textarea),
     desc_en (textarea), PIN (4-8 numeric digits, required)
   - doFamilyProfile() inserts into families table:
       { name, since, desc_ro, desc_en, pin_hash: btoa(pin),
         village: 'Calnic', owner_id: user.id }
     On success: window.location = 'dashboard.html'
     On error: shows error in #step2Msg .form-msg
   - doLogin() now checks families row after signInWithPassword:
       if family row exists  -> redirect to dashboard.html
       if no family row yet  -> show Step 2 form
   - localStorage persistence for language added

2. dashboard.html — new file (private family dashboard)
   - Auth-gated: listens for 'supabase:ready', calls getUser()
     If no user -> window.location = 'login.html'
     If user -> loads their families row
     If no families row -> shows "Complete profile" button
   - 4 collapsible sections (+/- toggle):
     Section 1 — Family Story
       Edit name, since, desc_ro, desc_en
       Save -> supabase.from('families').update().eq('id', familyId)
     Section 2 — Family Members
       List from supabase.from('members').select('*').eq('family_id')
       Each row: name, role, birth_year, death_year, is_deceased toggle,
       delete button
       Add member form: name (req), role, birth_year, death_year,
       is_deceased checkbox
     Section 3 — Family Timeline
       List from supabase.from('timeline').select().eq('family_id')
       .order('year')
       Each entry: year, text_ro, text_en, delete button
       Add entry form: year (req), text_ro (req), text_en
     Section 4 — Privacy & PIN
       Visibility toggles: show_members, show_generations, show_photos,
       show_since
       Change PIN form (new PIN -> btoa hash -> update pin_hash)
   - Language persistence: localStorage('calnic-lang')
   - Full bilingual support (RO/EN toggle)
   - Same Obsidian & Gold design as all other pages

3. familiile.html — Supabase wiring
   - CSS spinner (#gridLoading) shown while Supabase query runs
   - On success: real family cards rendered, statFamiliiNum updated
     from families.length
   - On error/empty: spinner hidden, demo data rendered immediately
     (buildAlphaBar + filterFamilies called)
   - Language persistence: localStorage('calnic-lang')

4. familiile-familie.html — Supabase wiring + fixes
   - Accepts both ?id= and ?family= URL params (backward compat)
   - Redirects to familiile.html if neither param is present
   - Loads real family data from Supabase on 'supabase:ready'
   - Falls back to Popescu demo data if family not found
   - setLang() now re-renders #fam-story in correct language
   - setLang() calls renderTimeline() to update all dynamic content
   - Language persistence: localStorage('calnic-lang')
   - setLang(currentLang) called on page load

5. Language persistence — all updated pages
   Pattern applied to every updated page:
     var currentLang = localStorage.getItem('calnic-lang') || 'ro';
     function setLang(lang) {
       currentLang = lang;
       localStorage.setItem('calnic-lang', lang);
       // ... rest of setLang
     }
     setLang(currentLang); // at page load

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ALL PAGES — CURRENT STATE (V8)
────────────────────────────────

1. index.html — Homepage
   - Mission, who it's for (4 cards), how it works (5 steps)
   - Public vs Private table, features grid (7 cards)
   - Free forever, anonymous about section
   - CTA -> familiile.html
   - Language persistence: DONE

2. istorie.html — Village History
   - Full timeline prehistory -> 1999 UNESCO
   - 20 rotating "Did you know?" facts
   - Cetatea, Church, Saxon + Romanian communities
   - Language persistence: TODO (still uses setLang('ro') hardcoded)

3. familiile.html — Families Directory
   - Search, filter, sort, alphabet bar
   - Gold border cards with Transylvanian SVG motif
   - Stats sidebar: familii count (live from Supabase), 312 locuitori
   - Supabase wiring: DONE (loads real families, spinner, fallback)
   - Language persistence: DONE
   - Links to: familiile-familie.html?id=UUID

4. galerie.html — Photo Gallery
   - Featured photo, recently added strip, 7 categories
   - Lightbox with zoom/download/share
   - Upload form with drag/drop
   - Placeholder SVG photos (to be replaced with Supabase Storage)
   - Language persistence: PARTIAL (reads localStorage but does not
     write it inside setLang, and init calls are not via setLang)
   - Supabase photo wiring: TODO

5. genealogie.html — Genealogy Level 1 (selector)
   - Village tree banner (full width, featured first)
   - Family cards grid with search/filter/sort
   - PHASE 2 HOOK still present — Supabase wiring: TODO
   - Language persistence: TODO
   - Links to: genealogie-familie.html?family=ID

6. genealogie-familie.html — Genealogy Level 2+3
   - Full tree canvas (Variant C), person detail panel
   - Add person form, back button -> genealogie.html
   - PHASE 2 HOOK still present — Supabase wiring: TODO
   - Language persistence: TODO
   - URL: genealogie-familie.html?family=UUID

7. harta.html — Interactive Map
   - ArcGIS satellite + OpenStreetMap toggle
   - Animated fly-in, gold markers, guided tour
   - Leaflet CDN: jsDelivr (NOT unpkg)
   - Guard: friendly error if Leaflet fails to load
   - Language persistence: TODO (no localStorage at all)

8. contact.html — Contact Page
   - 6 topic chips, form fields, char counter (0/1200)
   - Real email via Formspree xpqygdjn (WORKING)
   - Loading state -> success / error handling
   - FAQ section links to intrebari.html
   - Language persistence: DONE

9. familiile-familie.html — Individual Family Profile
   - URL: familiile-familie.html?id=UUID (Supabase UUID)
   - Also accepts ?family= for backward compat; redirects if missing
   - Hero: avatar, name, village, stats, badges
   - Breadcrumb: Acasa > Familiile > Familia X
   - Story, members (deceased faded), public photos
   - Tree preview -> genealogie-familie.html?family=ID
   - Family connections + timeline
   - PIN private access box wired to check_family_pin() RPC
   - Privacy note shown as required
   - Supabase wiring: DONE
   - Language persistence: DONE

10. intrebari.html — FAQ Page
    - 18 questions in 7 categories (search + filter chips)
    - Categories: Inregistrare, Privat, Fotografii,
      Genealogie, Tehnic, Diaspora
    - Sidebar: page links + contact button
    - Language persistence: DONE

11. login.html — Auth Page
    - Tab 1: Login (email + password)
      On login: checks families row -> dashboard.html or Step 2
    - Tab 2: Register (email + password)
      After signUp: shows Step 2 family profile form
    - Step 2 fields: family name, since, desc_ro, desc_en, PIN
    - doFamilyProfile(): inserts families row, redirects to dashboard
    - Language persistence: DONE

12. dashboard.html — Private Family Dashboard (NEW in V8)
    - Auth-gated via supabase:ready + getUser()
    - 4 collapsible sections: Story / Members / Timeline / Privacy+PIN
    - All CRUD ops fully wired to Supabase
    - Language persistence: DONE (uses calnic-lang key)

13. 404.html — Custom Error Page
    - Branded 404 page matching site design
    - Language persistence: DONE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REMAINING TODO — PRIORITIZED
──────────────────────────────

HIGH PRIORITY:
  1. genealogie.html — Replace PHASE 2 HOOK with real Supabase fetch
     Pattern to use (same as familiile.html):
       document.addEventListener('supabase:ready', function() {
         window.supabase.from('families')
           .select('id,name,village,since,members_count,generations_count')
           .order('name')
           .then(function(res) {
             if (res.error || !res.data || !res.data.length) return;
             FAMILIES = res.data.map(function(r) { ... });
             filterFamilies();
           });
       });
     Also add <script src="supabase.js"></script> before </body>
     Also add language persistence (localStorage pattern)

  2. genealogie-familie.html — Replace PHASE 2 HOOK with Supabase fetch
     Pattern:
       document.addEventListener('supabase:ready', function() {
         var params = new URLSearchParams(window.location.search);
         var familyId = params.get('family');
         if (!window.supabase || !familyId) return;
         Promise.all([
           window.supabase.from('families').select('id,name,village')
             .eq('id', familyId).single(),
           window.supabase.from('members').select('*')
             .eq('family_id', familyId).order('birth_year')
         ]).then(function(results) {
           // map members to PEOPLE object used by tree
         });
       });
     Also add <script src="supabase.js"></script> before </body>
     Also add language persistence (localStorage pattern)

  3. galerie.html — Complete language persistence (partial fix needed):
     Inside setLang(), add:  localStorage.setItem('calnic-lang', lang);
     Replace the 4 standalone init calls at end of script:
       buildCatBar(); applyFilters(); buildRecent(); buildThisWeek();
     With a single:
       setLang(currentLang);

MEDIUM PRIORITY:
  4. harta.html — Add language persistence
     Inside window._setLang (the real setLang), add:
       localStorage.setItem('calnic-lang', lang);
     After the window.onload block closes, add:
       setLang(currentLang);

  5. istorie.html — Add language persistence
     Change:  var currentLang hardcoded init
     to:      var currentLang = localStorage.getItem('calnic-lang') || 'ro';
     Inside setLang(), add: localStorage.setItem('calnic-lang', lang);
     Change the init at bottom from setLang('ro') to setLang(currentLang)
     (setLangFull wrapper already exists)

  6. Photo upload -> Supabase Storage
     - Create a 'photos' bucket in Supabase Storage (public read)
     - Wire the existing drag/drop upload form in galerie.html to
       supabase.storage.from('photos').upload()
     - Store metadata in a new 'photos' table:
         id, family_id (nullable), path, caption_ro, caption_en,
         category, uploaded_at, uploader_id

  7. galerie.html — Load real photos from Supabase Storage
     - On 'supabase:ready', fetch from photos table
     - Replace placeholder SVGs with real signed URLs
     - Add <script src="supabase.js"></script> if not present

LOW PRIORITY:
  8. harta.html — Add real family location pins from database
     After Supabase is wired, fetch families with village coords
     and add Leaflet markers to the map.

  9. SEO — Add og:image meta with real village photo URL
     Add to <head> of all pages:
       <meta property="og:image" content="URL_TO_VILLAGE_PHOTO">

  10. cron-job.org keep-alive
      After Supabase fully wired, set up a cron job at cron-job.org
      to ping the Supabase URL every 3 days to prevent free tier sleep.

  11. Domain — Buy .ro domain and connect to GitHub Pages
      Do this absolutely last.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LANGUAGE PERSISTENCE PATTERN (critical — use everywhere)
──────────────────────────────────────────────────────────
localStorage key: 'calnic-lang'   <-- NEVER use 'lang' alone

Standard pattern for ALL pages:

  /* at top of <script> block */
  var currentLang = localStorage.getItem('calnic-lang') || 'ro';

  function setLang(lang) {
    currentLang = lang;
    localStorage.setItem('calnic-lang', lang);    // <-- persist
    document.getElementById('btn-ro').classList.toggle('active', lang === 'ro');
    document.getElementById('btn-en').classList.toggle('active', lang === 'en');
    document.querySelectorAll('[data-ro]').forEach(function(el) {
      var val = el.getAttribute('data-' + lang);
      if (val !== null) el.textContent = val;
    });
    // ... any page-specific re-renders (filterFamilies, buildCatBar, etc.)
  }

  document.getElementById('btn-ro').addEventListener('click', function() { setLang('ro'); });
  document.getElementById('btn-en').addEventListener('click', function() { setLang('en'); });
  setLang(currentLang);  // <-- call on load (replaces any direct init calls)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LANGUAGE PERSISTENCE STATUS PER PAGE
──────────────────────────────────────
index.html              DONE
404.html                DONE
contact.html            DONE
intrebari.html          DONE
login.html              DONE
familiile.html          DONE
familiile-familie.html  DONE
dashboard.html          DONE
galerie.html            PARTIAL — reads localStorage but missing write
                        and setLang(currentLang) init
harta.html              TODO
istorie.html            TODO
genealogie.html         TODO
genealogie-familie.html TODO

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUPABASE WIRING STATUS PER PAGE
─────────────────────────────────
familiile.html          DONE — loads real families, spinner, fallback
familiile-familie.html  DONE — loads by ?id=, falls back to demo
login.html              DONE — signUp, signIn, Step 2 profile create
dashboard.html          DONE — getUser, CRUD story/members/timeline/PIN
genealogie.html         TODO — PHASE 2 HOOK still present
genealogie-familie.html TODO — PHASE 2 HOOK still present
galerie.html            TODO — photo upload + load from Storage
harta.html              TODO — family location pins (low priority)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

NAV STRUCTURE — ALL PAGES (unchanged)
───────────────────────────────────────
8 links, identical on every page, exact order:

  Acasa | Istoria Satului | Familiile | Genealogie | Harta |
  Galerie | Contact | Intrebari | [Login — far right]

EN versions:
  Homepage | Village History | Families | Genealogy | Map |
  Gallery | Contact | FAQ | [Family Login]

Active state: each page highlights its own nav link.
  genealogie-familie.html -> highlights Genealogie
  familiile-familie.html  -> NO active link (sub-page)
  dashboard.html          -> highlights Login (logged-in state)

Mobile: hamburger button -> #navMobileMenu full-width dropdown

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DESIGN STYLE — CRITICAL (unchanged)
──────────────────────────────────────
CSS Variables (same in every file — never change these):
--bg:      #0a0a0a    --s1:     #111111    --s2:    #161616
--s3:      #1c1c1c    --b1:     #2a2a2a    --b2:    #383838
--gold:    #d4a84a    --gold-l: #e0b84a    --gold-d:#b08030
--gold-m:  #7a5828    --text:   #f0e8d0    --muted: #a09070

Fonts: Playfair Display (headings) + EB Garamond (body)
Both from Google Fonts — always include in <head>

Banner: embedded as base64 in all pages (always loads offline)
SVG ornament: Transylvanian motif in header of every page

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TECHNICAL RULES — CRITICAL (unchanged)
─────────────────────────────────────────
- Pure HTML + CSS + JS only — no frameworks
- All files standalone .html, banner always base64 embedded
- NO Romanian special chars inside JS strings (use Unicode escapes)
- NO apostrophes inside single-quoted JS strings
- NO onerror="this.style.display='none'" pattern
- External libraries always in <head>
- Map/heavy JS always in window.addEventListener('load')
- Leaflet CDN: cdn.jsdelivr.net/npm/leaflet@1.9.4/ ONLY
- setLang() must update ALL visible text + all dynamic content
- Scroll buttons use style.opacity (not CSS classes)
- Always run node --check before delivering
- OG meta URLs: https://angelofsoul.github.io/ (never netlify)
- supabase.js must be loaded as last <script> before </body>
- Always show upload link after every file delivery
- Treat site as fully live — no "coming soon" framing

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VILLAGE INFORMATION
────────────────────
Village:    Calnic (German: Kelling, Hungarian: Kalnok)
County:     Alba, Transilvania, Romania
Founded:    First documented 1269
Altitude:   380m
Citadel:    Cetatea Calnic — UNESCO 1999
            lat: 45.8082, lng: 23.6438
Center:     lat: 45.8089, lng: 23.6447
Hamlet:     Deal (documented 1366)
Economy:    Beekeeping, fruit growing, viticulture
History:    Saxons (left bank) + Romanians (right bank)
            Saxons emigrated to Germany in 1980s

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PAGE CONNECTIONS MAP
─────────────────────
index.html
  └-> familiile.html (CTA + features card)
  └-> all pages (features grid)

familiile.html
  └-> familiile-familie.html?id=UUID

familiile-familie.html?id=UUID
  └-> genealogie-familie.html?family=UUID
  └-> galerie.html (photos)
  └-> familiile.html (breadcrumb)
  └-> familiile-familie.html?id=OTHER

genealogie.html
  └-> genealogie-familie.html?family=UUID

genealogie-familie.html?family=UUID
  └-> genealogie.html (back button)

login.html
  └-> dashboard.html (after login or Step 2 complete)

dashboard.html
  └-> login.html (if not authenticated)
  └-> familiile-familie.html (view public profile)

contact.html
  └-> intrebari.html

intrebari.html
  └-> contact.html
  └-> all pages (sidebar links)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TO START NEXT SESSION
──────────────────────
1. Open new chat
2. Upload or paste CALNIC_PROJECT_BRIEFING_V8.txt
3. Say what you want to build next

SUGGESTED NEXT (in order of priority):
  1. Wire genealogie.html to Supabase + fix language persistence
  2. Wire genealogie-familie.html to Supabase + fix language persistence
  3. Complete galerie.html language persistence (small fix)
  4. Fix harta.html + istorie.html language persistence (small fixes)
  5. Photo upload + galerie.html Supabase Storage wiring

=======================================================================
